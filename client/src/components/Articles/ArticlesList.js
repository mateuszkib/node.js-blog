import React from "react";
import PropTypes from "prop-types";

const ArticlesList = (props) => {
    return (
        <div className="container">
            <div className="col-8 offset-lg-2">Articles</div>
        </div>
    );
};

ArticlesList.propTypes = {};

export default ArticlesList;
